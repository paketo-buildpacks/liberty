<?xml version="1.0" encoding="UTF-8"?>
<server>
  <!-- Enable user features -->
  <featureManager>
    {{ range $val := . }}
        <feature>{{ $val }}</feature>
    {{ end }}
  </featureManager>
</server>
